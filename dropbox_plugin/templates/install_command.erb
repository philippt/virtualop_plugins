description "installation script for <%= service_name %>"

param :machine
param! "domain", "the domain at which the service should be available"
param! "service_root", "fully qualified path to the location of the service"

on_machine do |machine, params|
  
end
