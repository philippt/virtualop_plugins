###############################################################################
###############################################################################
#
# HOST DEFINITIONS
#
###############################################################################
###############################################################################

<% options = @op.ssh_options_for_machine("machine" => machine.name) %>
define host{
        use                     windows-host;
        host_name               <%= machine.name %>
        alias                   <%= machine.name %>
        address                 <%= machine.ipaddress %>
        }


###############################################################################
###############################################################################
#
# SERVICE DEFINITIONS
#
###############################################################################
###############################################################################


define service{
        use                             generic-service
        host_name                       <%= machine.name %>
        service_description             Memory
        check_command                   check_snmp_windows_mem!80!95
        notification_options            n
        }

define service{
        use                             disk-template
        host_name                       <%= machine.name %>
        service_description             Disk - C:
        check_command                   check_snmp_storage_windows_used!C:!85!95
        }

#define service{
#        use                             disk-template
#        host_name                       sbk-hms-qs-app2
#        service_description             Disk - F:
#        check_command                   check_snmp_storage_windows_used!F:!80!95
#        }

define service{
        use                             generic-service
        host_name                       <%= machine.name %>
        service_description             Load
        check_command                   check_snmp_cpu_windows!85!95
        }

define service{
        use                             generic-service
        host_name                       <%= machine.name %>
        service_description             Virtual Memory
        check_command                   check_snmp_windows_virtmemory!50!90
        notification_options            n
        }

define service{
        use                             service-template-nop
        host_name                       <%= machine.name %>
        service_description             OfficeScan NT Listener
        check_command                   check_snmp_process!TmListen.exe
        }

define service{
        use                             service-template-nop
        host_name                       <%= machine.name %>
        service_description             OfficeScan NT Realtime Scan
        check_command                   check_snmp_process_regex![nN][tT][rR][tT][sS]can.exe
        }

define service{
        use                             service-template-nop
        host_name                       <%= machine.name %>
        service_description             Prozess VMWare Tools
        check_command                   check_snmp_process!vmtoolsd.exe
        }

define service{
        use                             generic-service
        host_name                       <%= machine.name %>
        service_description             Port 8080
        check_command                   check_tcp!8080
        }
