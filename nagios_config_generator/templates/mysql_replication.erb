define service {
        use                     generic-service,srv-pnp
        host_name               <%= machine.name %>
        service_description     mysql replication slave io 
        check_command           check_mysql_health!$HOSTADDRESS$!<%= config_string('mysql_check_user') %>!<%= config_string('mysql_check_password') %>!slave-io-running
}

define service {
        use                     generic-service,srv-pnp
        host_name               <%= machine.name %>
        service_description     mysql replication slave sql 
        check_command           check_mysql_health!$HOSTADDRESS$!<%= config_string('mysql_check_user') %>!<%= config_string('mysql_check_password') %>!slave-sql-running
}

define service {
        use                     generic-service,srv-pnp
        host_name               <%= machine.name %>
        service_description     mysql replication slave lag 
        check_command           check_mysql_health!$HOSTADDRESS$!<%= config_string('mysql_check_user') %>!<%= config_string('mysql_check_password') %>!slave-lag
}